/**
 * @module routes/controllers/API_removeFromCart
 * @fileoverview API_removeFromCart route's controller. Exports
 *    functions to be used by each route handler.
 * @exports {Object} Functions to attach to the `API_removeFromCart` router.
 * @require session-helper
 */

 /* sessions
  * This is to help with handling sessions to maintain cart and auth
  */
 const sessions = require('../../scripts/session-helper.js');


/**
 * @function sendAPI_removeFromCart
 * @description Send the base page rendered by `Handlebars.js`. Compilation
 *    of HBS templates is handled by the HBS engine.
 * @param {Object} req The request object generated by `Express`.
 * @param {Object} res The response object generated by `Express`.
 * @param {Function} next The function to call when this method is done executing
 *    and does not return or render anything (no `res` methods called).
 */
const removeFromCart = (req, res, next) => {
  // make sure an admin is making the call
  var sessionId = req.cookies["sessionId"];
  var itemId = req.body["itemId"];
  var amount = req.body["amount"];
  var isAdding = false;
  sessions.handleSessionUpdateCart(sessionId)
    .then()
};


module.exports = {
  removeFromCart,
};
